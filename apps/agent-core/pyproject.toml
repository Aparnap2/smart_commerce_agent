[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "integration: marks tests as integration tests (Docker + real LLM)",
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
